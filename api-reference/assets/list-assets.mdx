---
title: 'List Assets'
description: 'Retrieve a list of crypto assets with comprehensive data'
---

## GET /v3/assets

Retrieve a paginated list of crypto assets with optional filtering and expansion of related data.

### Request

```bash
GET https://rest.staging.blockworksresearch.com/v3/assets
```

### Authentication

<CodeGroup>

```bash cURL
curl -H "x-api-key: YOUR_API_KEY" \
  "https://rest.staging.blockworksresearch.com/v3/assets"
```

```typescript TypeScript
const response = await fetch('https://rest.staging.blockworksresearch.com/v3/assets', {
  headers: {
    'x-api-key': 'YOUR_API_KEY',
    'Content-Type': 'application/json'
  }
});
```

```python Python
import requests

headers = {
    'x-api-key': 'YOUR_API_KEY',
    'Content-Type': 'application/json'
}

response = requests.get(
    'https://rest.staging.blockworksresearch.com/v3/assets',
    headers=headers
)
```

</CodeGroup>

### Query Parameters

| Parameter | Type | Description | Default |
|-----------|------|-------------|---------|
| `page` | integer | Page number (1-indexed) | 1 |
| `limit` | integer | Items per page (max 100) | 10 |
| `code` | string | Filter by asset code/symbol | - |
| `slug` | string | Filter by asset slug | - |
| `is_supported` | boolean | Filter by supported status | - |
| `sector` | string | Filter by sector | - |
| `expand` | array | Expand related data | - |
| `sort` | string | Sort by field | - |

### Expansion Options

Use the `expand` parameter to include additional data:

| Value | Description |
|-------|-------------|
| `price` | Current price information |
| `market_cap` | Market capitalization data |
| `sector` | Sector classification |
| `addresses` | Contract addresses |
| `markets` | Trading market information |

### Examples

#### Basic Request

<CodeGroup>

```bash cURL
curl -H "x-api-key: YOUR_API_KEY" \
  "https://rest.staging.blockworksresearch.com/v3/assets?limit=20"
```

```typescript TypeScript
const response = await fetch('https://rest.staging.blockworksresearch.com/v3/assets?limit=20', {
  headers: {
    'x-api-key': 'YOUR_API_KEY',
    'Content-Type': 'application/json'
  }
});

const data = await response.json();
console.log(`Found ${data.data.length} assets`);
```

```python Python
import requests

headers = {'x-api-key': 'YOUR_API_KEY'}
params = {'limit': 20}

response = requests.get(
    'https://rest.staging.blockworksresearch.com/v3/assets',
    headers=headers,
    params=params
)

data = response.json()
print(f"Found {len(data['data'])} assets")
```

</CodeGroup>

#### With Expanded Data

<CodeGroup>

```bash cURL
curl -H "x-api-key: YOUR_API_KEY" \
  "https://rest.staging.blockworksresearch.com/v3/assets?expand=price,market_cap,sector&limit=10"
```

```typescript TypeScript
const response = await fetch('https://rest.staging.blockworksresearch.com/v3/assets?expand=price,market_cap,sector&limit=10', {
  headers: {
    'x-api-key': 'YOUR_API_KEY',
    'Content-Type': 'application/json'
  }
});

const data = await response.json();
data.data.forEach(asset => {
  console.log(`${asset.title}: $${asset.price?.usd || 'N/A'} (${asset.sector?.title || 'N/A'})`);
});
```

```python Python
headers = {'x-api-key': 'YOUR_API_KEY'}
params = {
    'expand': 'price,market_cap,sector',
    'limit': 10
}

response = requests.get(
    'https://rest.staging.blockworksresearch.com/v3/assets',
    headers=headers,
    params=params
)

data = response.json()
for asset in data['data']:
    price = asset.get('price', {}).get('usd', 'N/A')
    sector = asset.get('sector', {}).get('title', 'N/A')
    print(f"{asset['title']}: ${price} ({sector})")
```

</CodeGroup>

#### Filter by Sector

<CodeGroup>

```bash cURL
curl -H "x-api-key: YOUR_API_KEY" \
  "https://rest.staging.blockworksresearch.com/v3/assets?sector=DeFi&expand=price,market_cap"
```

```typescript TypeScript
const response = await fetch('https://rest.staging.blockworksresearch.com/v3/assets?sector=DeFi&expand=price,market_cap', {
  headers: {
    'x-api-key': 'YOUR_API_KEY',
    'Content-Type': 'application/json'
  }
});

const data = await response.json();
console.log(`Found ${data.data.length} DeFi assets`);
```

```python Python
headers = {'x-api-key': 'YOUR_API_KEY'}
params = {
    'sector': 'DeFi',
    'expand': 'price,market_cap'
}

response = requests.get(
    'https://rest.staging.blockworksresearch.com/v3/assets',
    headers=headers,
    params=params
)

data = response.json()
print(f"Found {len(data['data'])} DeFi assets")
```

</CodeGroup>

### Response

#### Success Response (200 OK)

```json
{
  "data": [
    {
      "id": 1,
      "code": "NEAR",
      "title": "NEAR Protocol",
      "slug": "near-protocol",
      "tag_line": "A scalable, developer-friendly blockchain",
      "description": "NEAR is a decentralized development platform that uses a Proof-of-Stake (PoS) consensus mechanism...",
      "image_url": "https://coin-images.coingecko.com/coins/images/10365/large/near.jpg?1696510367",
      "legacy_sector": "Smart Contract Platforms",
      "category": "Infrastructure",
      "is_supported": true,
      "updated_at": 1747346527,
      "sector_id": 40,
      "type": "Infrastructure",
      "sector": {
        "id": 40,
        "title": "L1",
        "created_at": 1698971397
      },
      "market_cap": {
        "asset_id": 1,
        "rank": 40,
        "usd": 3519747898,
        "dominance": 0.285261,
        "percent_change_btc_1_h": 0.0977395,
        "percent_change_btc_24_h": 3.23624,
        "percent_change_usd_1_h": -0.552975,
        "percent_change_usd_24_h": -5.59772,
        "updated_at": 1747346527
      },
      "price": {
        "asset_id": 1,
        "usd": 2.89,
        "btc": 0.0001053774710807,
        "eth": 0.0020572563705035,
        "sparkline_7d": "[2.6113154693028338,2.6243590389934597,...]",
        "updated_at": 1747346527
      }
    }
  ],
  "total": 145309,
  "page": 1
}
```

#### Error Responses

<AccordionGroup>
  <Accordion title="400 Bad Request">
    ```json
    {
      "error": {
        "code": "INVALID_PARAMETER",
        "message": "Invalid expand parameter value",
        "details": {
          "invalid_values": ["invalid_field"]
        }
      }
    }
    ```
  </Accordion>
  
  <Accordion title="401 Unauthorized">
    ```json
    {
      "error": {
        "code": "UNAUTHORIZED",
        "message": "Invalid API key"
      }
    }
    ```
  </Accordion>
  
  <Accordion title="429 Too Many Requests">
    ```json
    {
      "error": {
        "code": "RATE_LIMIT_EXCEEDED",
        "message": "Rate limit exceeded",
        "details": {
          "limit": 1000,
          "remaining": 0,
          "reset": 1640995200
        }
      }
    }
    ```
  </Accordion>
</AccordionGroup>

### Response Fields

| Field | Type | Description |
|-------|------|-------------|
| `id` | number | Unique asset identifier |
| `code` | string | Asset code/symbol (e.g., "BTC", "ETH") |
| `title` | string | Full asset name |
| `slug` | string | URL-friendly asset identifier |
| `tag_line` | string | Brief description of the asset |
| `description` | string | Detailed asset description |
| `image_url` | string | URL to asset logo/image |
| `legacy_sector` | string | Legacy sector classification |
| `category` | string | Asset category (Infrastructure, Financial, etc.) |
| `is_supported` | boolean | Whether asset is supported |
| `updated_at` | number | Unix timestamp of last update |
| `sector_id` | number | Sector identifier |
| `type` | string | Asset type (Infrastructure, Application, etc.) |
| `sector` | object | Sector information (if expanded) |
| `market_cap` | object | Market cap data with USD value, rank, changes (if expanded) |
| `price` | object | Price data in USD, BTC, ETH with sparkline (if expanded) |
| `total` | number | Total number of assets available |
| `page` | number | Current page number |

### Common Use Cases

#### Portfolio Tracking

Get current prices for your portfolio assets:

```typescript
const portfolioSlugs = ['bitcoin', 'ethereum', 'solana'];
const slugFilter = portfolioSlugs.join(',');

const response = await fetch(`https://rest.staging.blockworksresearch.com/v3/assets?slug=${slugFilter}&expand=price,market_cap`, {
  headers: { 'x-api-key': 'YOUR_API_KEY' }
});
```

#### Sector Analysis

Analyze assets by sector:

```python
sectors = ['DeFi', 'Layer 1', 'Layer 2']
sector_data = {}

for sector in sectors:
    response = requests.get(
        'https://rest.staging.blockworksresearch.com/v3/assets',
        headers={'x-api-key': 'YOUR_API_KEY'},
        params={'sector': sector, 'expand': 'price,market_cap', 'limit': 50}
    )
    sector_data[sector] = response.json()['data']
```

#### Market Screening

Screen for assets meeting specific criteria:

```typescript
// Find DeFi assets with market cap > $1B
const response = await fetch('https://rest.staging.blockworksresearch.com/v3/assets?sector=DeFi&expand=market_cap&limit=100', {
  headers: { 'x-api-key': 'YOUR_API_KEY' }
});

const data = await response.json();
const largeCap = data.data.filter(asset => asset.market_cap > 1000000000);
```

### Rate Limiting

This endpoint is subject to rate limiting. Check the response headers:

```
X-RateLimit-Limit: 1000
X-RateLimit-Remaining: 999
X-RateLimit-Reset: 1640995200
```

### Next Steps

<CardGroup cols={2}>
  <Card
    title="Get Single Asset"
    icon="coin"
    href="/api-reference/assets/get-asset"
  >
    Get detailed information about a specific asset
  </Card>
  <Card
    title="TypeScript Examples"
    icon="code"
    href="/examples/typescript"
  >
    See complete TypeScript implementation examples
  </Card>
</CardGroup>