---
title: 'List Assets'
description: 'Retrieve a paginated list of crypto assets'
openapi: 'GET /v3/assets'
---

import { Endpoint } from '/snippets/endpoint.mdx'

<Endpoint method="GET" path="/v3/assets" size="xl" />

### Response Structure

The API returns a paginated response with the following structure:

```json
{
  "data": [
    {
      "id": 4,
      "code": "BTC",
      "title": "Bitcoin",
      "slug": "bitcoin",
      "tag_line": "A peer-to-peer electronic cash system",
      "description": "Bitcoin is the first distributed consensus-based...",
      "image_url": "https://coin-images.coingecko.com/coins/images/1/large/bitcoin.png?1696501400",
      "legacy_sector": "Currencies",
      "category": "Infrastructure",
      "is_supported": true,
      "updated_at": 1747346559,
      "sector_id": 40,
      "type": "Infrastructure"
    }
  ],
  "total": 1500,
  "page": 1,
  "limit": 10
}
```

## Making a Request

<CodeGroup>

```typescript TypeScript
const response = await fetch(
  'https://api.blockworksresearch.com/v3/assets?limit=20',
  { headers: { 'x-api-key': 'YOUR_API_KEY' } }
)

const data = await response.json()
```

```python Python
import requests

response = requests.get(
    'https://api.blockworksresearch.com/v3/assets',
    headers = { 'x-api-key': 'YOUR_API_KEY' },
    params = { 'limit': 20 }
)

data = response.json()
```

```bash cURL
curl -H "x-api-key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  "https://api.blockworksresearch.com/v3/assets?limit=20"
```

</CodeGroup>

### Query Parameters

| Parameter | Type | Description | Default |
|-----------|------|-------------|---------|
| `page` | integer | Page number (1-indexed) | 1 |
| `limit` | integer | Items per page (max 100) | 10 |
| `code` | string | Filter by asset code (e.g., "BTC") | - |
| `slug` | string | Filter by asset slug | - |
| `category` | string | Filter by asset category | - |
| `favorites` | boolean | Show only favorite assets | false |
| `expand` | array | Expand related data | - |

### Expansion Options

Use the `expand` parameter to include additional related data for each asset:

| Value | Description |
|-------|-------------|
| `price` | Current price information |
| `market_cap` | Market capitalization data |
| `sector` | Sector classification |
| `markets` | Trading market data |
| `supply` | Supply information |
| `is_favorite` | Favorite status |

### Examples

#### Basic Request

<CodeGroup>

```typescript TypeScript
const response = await fetch('https://api.blockworksresearch.com/v3/assets', {
  headers: { 'x-api-key': 'YOUR_API_KEY' }
})

const data = await response.json()
console.log(`Found ${data.total} assets`)
```

```python Python
import requests

response = requests.get(
    'https://api.blockworksresearch.com/v3/assets',
    headers={'x-api-key': 'YOUR_API_KEY'}
)

data = response.json()
print(f"Found {data['total']} assets")
```

```bash cURL
curl -H "x-api-key: YOUR_API_KEY" \
  "https://api.blockworksresearch.com/v3/assets"
```

</CodeGroup>

#### Filter by Category

<CodeGroup>

```typescript TypeScript
const response = await fetch('https://api.blockworksresearch.com/v3/assets?category=Infrastructure&limit=50', {
  headers: { 'x-api-key': 'YOUR_API_KEY' }
})

const data = await response.json()
```

```python Python
response = requests.get(
    'https://api.blockworksresearch.com/v3/assets',
    headers={'x-api-key': 'YOUR_API_KEY'},
    params={'category': 'Infrastructure', 'limit': 50}
)
```

```bash cURL
curl -H "x-api-key: YOUR_API_KEY" \
  "https://api.blockworksresearch.com/v3/assets?category=Infrastructure&limit=50"
```

</CodeGroup>

#### Expand Price Data

<CodeGroup>

```typescript TypeScript
const response = await fetch('https://api.blockworksresearch.com/v3/assets?expand=price,market_cap&limit=10', {
  headers: { 'x-api-key': 'YOUR_API_KEY' }
})

const data = await response.json()
```

```python Python
response = requests.get(
    'https://api.blockworksresearch.com/v3/assets',
    headers={'x-api-key': 'YOUR_API_KEY'},
    params={'expand': 'price,market_cap', 'limit': 10}
)
```

```bash cURL
curl -H "x-api-key: YOUR_API_KEY" \
  "https://api.blockworksresearch.com/v3/assets?expand=price,market_cap&limit=10"
```

</CodeGroup>