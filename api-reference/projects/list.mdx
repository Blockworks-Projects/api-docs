---
title: 'List Projects'
description: 'Retrieve a list of all available projects with their key metadata'
openapi: 'GET /v1/projects'
mode: 'wide'
---

## Overview

This endpoint returns a **paginated list of projects** supported by the API.
Each project includes its identifier, title, slug, description, and other metadata used across API endpoints.
Use this endpoint to **discover projects** before querying metrics, chains, or other detailed data.

## Example Request

<CodeGroup>

```bash cURL
curl -H "x-api-key: YOUR_API_KEY" \
  "https://api.blockworks.com/v1/projects?limit=20"
```

```typescript TypeScript
const response = await fetch(
  'https://api.blockworks.com/v1/projects?limit=20',
  { headers: { 'x-api-key': 'YOUR_API_KEY' } }
)
const data = await response.json()
```

```python Python
import requests

response = requests.get(
    'https://api.blockworks.com/v1/projects',
    headers={'x-api-key': 'YOUR_API_KEY'},
    params={'limit': 20}
)
data = response.json()
```

</CodeGroup>

### Example Response

A successful response returns a paginated list of projects.

```json
{
  "data": [
    {
      "id": 289,
      "title": "Ethereum",
      "tagline": null,
      "description": null,
      "slug": "ethereum",
      "url": null,
      "image_url": "https://blockworks-website-assets.s3.us-east-2.amazonaws.com/projects/project-images/ethereum_logo.png",
      "updated_at": 1761939081,
      "asset": {
        "id": 2,
        "code": "ETH",
        "title": "Ethereum",
        "slug": "ethereum",
        "tag_line": "A decentralized computing platform",
        "description": "Ethereum is a distributed blockchain computing platform for smart contracts and decentralized applications.",
        "image_url": "https://blockworks-assets.imgix.net/279/large/ethereum.png?1696501628",
        "category": "Infrastructure",
        "updated_at": 1761760828,
        "type": "Infrastructure",
        "sector": "L1",
        "market_cap": {
          "rank": 2,
          "usd": 401590918646,
          "dominance": 11.009
        },
        "price": {
          "usd": 3311.76,
          "btc": 0.032660677126993366,
          "eth": 1
        }
      }
    },
    ... // more projects
  ],
  "total": 1081,
  "page": 1
}
```

## Supported Options

| Name | Type | Details |
| - | - | - |
| `limit` | query | The number of projects to return (max 100). |
| `page` | query | The page number to return. |
| `search` | query | Search term to filter projects by title or slug. |
| `sparkline` | query | Include sparkline data (e.g., '7d' for 7-day price history). |

## Response Fields

| Field | Type | Description |
|---|---|---|
| `id` | number | Unique project identifier |
| `title` | string | Full project name |
| `tagline` | string \| null | Short tagline for the project |
| `description` | string \| null | Detailed project description |
| `slug` | string | URL-friendly identifier |
| `url` | string \| null | Project website URL |
| `image_url` | string | URL to project logo/image |
| `updated_at` | number | Unix timestamp of last update |
| `asset` | object | Associated asset information (includes code, title, category, type, sector, and optional market_cap/price data) |

## Notes

- Results are **paginated** — use `limit` and `page` query parameters to navigate.
- Project identifiers returned here can be used in other API endpoints.

## Related Endpoints

- [Get Single Project](/api-reference/projects/get-single)
- [Get Project Chains](/api-reference/projects/get-chains)
