---
title: 'Real Economic Value'
description: 'Retrieve Real Economic Value Metrics'
openapi: 'GET /v3/metrics/issuance'
---

import { Endpoint } from '/snippets/endpoint.mdx'
import Request from '/snippets/request.mdx'

<Endpoint method="GET" path="/v3/metrics/issuance" size="xl" />

## Basic Example

<CodeGroup>

```typescript TypeScript
const response = await fetch(
  'https://rest.staging.blockworksresearch.com/v3/metrics/issuance?project=Ethereum', {
  headers: { 'x-api-key': 'YOUR_API_KEY' }
})

const data = await response.json()
```

```python Python
import requests

response = requests.get(
    'https://rest.staging.blockworksresearch.com/v3/metrics/issuance',
    headers={'x-api-key': 'YOUR_API_KEY'},
    params={'project': 'Ethereum'}
)

data = response.json()
```

```bash cURL
curl -H "x-api-key: YOUR_API_KEY" \
  "https://rest.staging.blockworksresearch.com/v3/metrics/issuance?project=Ethereum"
```

</CodeGroup>


### Response Structure

Returns a timeseries of active addresses for a project, grouped by date.

```json
{
  "Ethereum": [
    {
      "date": "2025-07-23",
      "value": 106994
    },
    {
      "date": "2025-07-22",
      "value": 510353
    },
    {
      "date": "2025-07-21",
      "value": 493831
    },
    {
      "date": "2025-07-20",
      "value": 443241
    },
    {
      "date": "2025-07-19",
      "value": 450414
    },
    {
      "date": "2025-07-18",
      "value": 506576
    }
  ]
}
```

## Configuration

### Path Parameters

| Parameter | Type | Description | Required |
|-----------|------|-------------|----------|
| `identifier` | `string` | Metric identifier | Yes |

### Query Parameters

| Parameter | Type | Description | Default |
|-----------|------|-------------|---------|
| `project` | string | Project identifier | Required |
| `start_date` | string | Start date (YYYY-MM-DD) | 30 days ago |
| `end_date` | string | End date (YYYY-MM-DD) | Today |
| `aggregation` | string | Data aggregation (daily, weekly, monthly) | daily |
| `format` | string | Response format (json, csv) | json |
yu